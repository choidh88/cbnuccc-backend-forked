Run the project scaffold helper with the provided name.

Use:

```bash
./slash /new "$ARGUMENTS"
```

This creates matching Model, Dto, Repository, Service, and Controller files under `src/main/java/com/cbnuccc/cbnuccc`.

Some Codex clients reserve `/new` as a built-in command. If this prompt is not shown, use `/new-scaffold` instead.
